const { articleRouter } = require('./article');
const { searchRouter } = require('./search');
const { userRouter } = require('./user');

module.exports = {
  articleRouter,
  searchRouter,
  userRouter,
};
